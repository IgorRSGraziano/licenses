<div class="mx-auto md:w-2/3 w-full">
  <h1 class="font-bold text-4xl"><%= "#{t 'actions.new'} #{Customer.h}" %></h1>

  <%= render "form", customer: @customer %>

  <%= link_to "#{t 'actions.back_to'} #{Customer.h}", customers_path, class: "ml-2 rounded-lg py-3 px-5 bg-gray-100 inline-block font-medium" %>
</div>
